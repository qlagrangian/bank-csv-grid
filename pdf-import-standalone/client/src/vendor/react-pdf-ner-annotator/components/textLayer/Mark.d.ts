import React from 'react';
import { Annotation } from '../../interfaces/annotation';
interface Props {
    token: string;
    annotation: Annotation;
}
declare const _default: React.MemoExoticComponent<({ token, annotation }: Props) => React.JSX.Element>;
export default _default;
