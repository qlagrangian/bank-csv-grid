import React from 'react';
interface Props {
    pdfScale: number;
    pageNumber: number;
}
declare const AreaLayer: ({ pdfScale, pageNumber }: Props) => React.JSX.Element;
export default AreaLayer;
