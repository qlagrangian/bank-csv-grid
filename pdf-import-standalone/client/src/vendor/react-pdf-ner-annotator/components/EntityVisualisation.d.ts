import React from 'react';
import { Entity } from '../interfaces/entity';
type Props = {
    entity?: Entity;
};
declare const EntityVisualisation: ({ entity }: Props) => React.JSX.Element;
export default EntityVisualisation;
